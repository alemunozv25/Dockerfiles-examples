FROM python:3.12-alpine

ADD index.html index.html

ADD webserver.py webserver.py

EXPOSE 8383

# Show stdout from container to host
ENV PYTHONUNBUFFERED=1

CMD [ "python3", "webserver.py", "--host=0.0.0.0"]
