# syntax=docker/dockerfile:1
FROM busybox
LABEL maintainer="Alex Munoz"
ADD echo-good-day.sh /
RUN chmod +x /echo-good-day.sh 
#Executing a RUN command happens when building the image, and is not available in the final image generated
#RUN echo ---
CMD ["/echo-good-day.sh"]
